// 1. On a click on the 'Fetch friends' button, get the 'friends.json' file
// using $.getJSON

// 2. Populate the 'friends' table in the page using $().append


// 3. Take into account the namefilter input: only show the friends that contain the
// text in the namefilter input. The search should be case insensitive.
// The search should be done on the server side by passing the namefilter as a query parameter.


// 4. Do the search as the user types in the namefilter input. This means that the search
// should be done on every keyup event.
